// 中文习惯排版：正文仿宋；标题大于正文字号用 Noto 宋体，等于正文字号用黑体；着重/加粗用楷体
#let body-size = 12pt
#let 宋体 = ((name: "Times New Roman", covers: "latin-in-cjk"), "Noto Serif CJK SC")
#let 黑体 = ((name: "Times New Roman", covers: "latin-in-cjk"), "SimHei")
#let 楷体 = ((name: "Times New Roman", covers: "latin-in-cjk"), "KaiTi")
#let 仿宋 = ((name: "Times New Roman", covers: "latin-in-cjk"), "FangSong")

#set document(
  title: "AI Proofreader——可深度配置的图书审校工作台",
  author: "黄富雄",
)
#set page(
  paper: "a4",
  margin: (x: 2.5cm, y: 2.5cm),
  numbering: "1",
  number-align: center,
)
#set text(
  font: 仿宋,
  size: body-size,
  lang: "zh",
  region: "CN",
  cjk-latin-spacing: auto,
)
#set par(
  justify: true,
  first-line-indent: (amount: 2em, all: true),
)
#set heading(numbering: none)

// 标题：大于正文字号 → Noto 宋体；等于正文字号 → 黑体
#show heading: it => {
  let size = if it.level == 1 { 1.5em }
    else if it.level == 2 { 1.2em }
    else if it.level == 3 { 1.1em }
    else { 1em }
  let font = if size > 1em { 宋体 } else { 黑体 }
  set text(font: font, size: size, weight: "regular")
  if it.level == 1 {
    align(center, block(above: 1.2em, below: 1.2em, it.body))
  } else {
    block(above: 1.2em, below: 0.8em, it.body)
  }
}

// 着重 / 加粗 → 楷体
#show strong: it => text(font: 楷体, weight: "regular", it.body)
#show emph: it => text(font: 楷体, style: "normal", it.body)

#show link: it => text(fill: rgb("#0645AD"), it)
#show raw: set text(font: ("Consolas", "FangSong"), size: 0.92em)

// 图/表阿拉伯数字编号（图1、表2等），题注与正文引用均自动生成，避免手写序号出错
#set figure(numbering: "1")
#show figure.where(kind: image): set figure(supplement: [图])
#show figure.where(kind: table): set figure(supplement: [表])
#show figure.where(kind: table): set figure.caption(position: top) // 表题在上、图题在下
#show figure: set block(above: 1.2em, below: 1.2em) // 图表前后留白
#show figure.caption: it => {
  set text(font: 楷体)
  context [
    #it.supplement#it.counter.display(it.numbering)
    #if it.body != [] [#h(1em)#it.body]
  ]
}
#show ref: it => {
  let el = it.element
  if el != none and el.func() == figure {
    let n = counter(figure.where(kind: el.kind)).at(el.location())
    link(el.location(), [#el.supplement#numbering(el.numbering, ..n)])
  } else {
    it
  }
}

// 插图：不超过版心指定比例；小于该宽度时按原图像素尺寸显示，避免小截图被放大发糊
#let 插图(path, 题注, max-width: 1) = {
  figure(
    layout(size => {
      let limit = size.width * max-width
      let natural = measure(image(path))
      if natural.width > limit {
        image(path, width: limit, scaling: "smooth")
      } else {
        image(path, scaling: "smooth")
      }
    }),
    caption: 题注,
  )
}

#v(2em)

#align(center)[
  #set par(first-line-indent: 0pt, spacing: 0.6em)
  #text(font: 宋体, size: 2.5em)[AI Proofreader] \
  #text(font: 仿宋, size: 1.5em)[#h(10em)——可深度配置的图书审校工作台]
]

#align(right)[
  #v(0.8em)
  语文出版社　黄富雄 \
  2026年9月#h(2em)
]

#v(2em)

== 一、审校工作的痛点

审校工作头绪多，琐碎繁杂，相当耗费精力，很容易出现疏漏和错误，编校人员常有心理负担。

审校一份书稿，图书编校人员要在一定时间内完成以下工作：按顺序通读书稿，再辅以专项检查，找出潜在问题；在全书语境或相关多书语境中核实问题；对照各种外部参考资料核实问题，如工具书、在线文本库、相关专著与论文等；对已核实的问题，拟订修改方案，并在纸稿或电子稿上改正；机改后逐一核红，确保排版文件中已得到正确体现；整理加工记录，留存审改档案。

就说一致性问题吧，发现问题后，要在全书语境中核实，对照外部参考资料复核，更要用心进行多重判断：前后说法不一致、同一人名写法不同、练习题与答案不对应、引文与原文有出入……教参、练习册、注释类图书尤其如此：题目与答案、正文与注释、古诗文与权威解释，要放在一起比对核查；配套教材、词典、在线文本库和相关专著论文，常常要轮番打开查阅。除了从前到后的通读审改，通常还要进行专项核对，比如核对目录、页眉页脚、栏目名称，或对注释、拼音、人物信息等进行专项强化审校。

再说说看似小事的核红。修改方案落实到纸稿或电子稿之后，仍须在各校次认真核红，核对排版文件是否已按修改稿改正，尤其是最终印刷版。核红很容易出问题，尤其是稿件质量不高、改动较多时，纸稿上改过了，排印版里却没改或改错了，这种情况常有发生。更严重的是，因使用文件版本错误，上一校次改过的内容，下一校次可能又恢复了原样，或将三校改到了一校上。

最后是根据全部审校工作整理出加工记录，并留存审改档案。整理加工记录相当费工，哪些记录、哪些忽略颇费思量，逐条输入、说明总让人感觉在做“无用功”，而留存纸质过程稿也存在占用空间、不易复查、难以再利用等问题。

以上种种，都不是“再仔细一点儿，再耐心一点儿”的自我激励能解决的。书稿少说十几万字，必须逐字审读，时间稍紧，或精神有所疲怠，就容易出现疏漏。

因此，计算机辅助审校一直是编校人员的理想。如今，记忆力极强、永不懈怠的大语言模型的爆发，让我们看到了希望。

== 二、“一键出结果、侧栏点标签”式审校应用的问题

近两年，市面上除了传统审校软件黑马校对，又出现了多款AI审校应用，比如方正智能审校、果麦AI校对王、蜜度校对通等。

传统审校软件基于字词和表达式级别的正误对查找替换，完全没有上下文语义理解能力，召回率、准确率很低，常常两三百处标红只有一处正确改动，编校人员的审核负担很重，收益很小。市面上流行的AI审校应用，得益于大语言模型似乎无限的生成能力，确实能发现并改正很多书稿问题，能大大减轻编辑的审校压力。

但这些AI审校应用往往是“一键出结果、侧栏点标签”式的，一方面入门简单，几乎不用培训、学习；另一方面也带来很多问题：

+ 改错和润色的边界模糊，无法配置，常常改动过多，导致审核负担很大。
+ PDF文件解析策略有限，断行、分页常常破坏语境，复杂版面无法还原语流，稍有字距便引入空格，方正排版软件常用的半角标点、字符替代也会引入批量错误。这些问题导致大量无效修改。
+ 审校单元无从知晓，无法确定语境是否完整，是否过长；语流是否连贯，是否关系清楚。而这些是AI审校质量的关键因素。
+ 采用侧栏标签说明对应版面着色锚点的审核界面与方式，效率很低，负担相当重。编辑审核时必须逐一打开标签，找到对应的原文，对照原文阅读、理解标签中的改动方案，最后通过想象将其带入原文语境，判断是否正确。就界面容量而言，一屏只能显示二三十个标签；从实际情况看，版面上的着色锚点往往互相叠压，无法区分和操作。如此冗长的操作与决策链条，加上界面限制，导致审核的思维复杂度很高，心智负担重，审核效率低。
+ 缺乏灵活性，不能适应多样化的审校场景。有些书稿是名家力作，只需改错；有些书稿文字质量较差，需要在改错的基础上适当润色。有些儿童读物每页即为独立语境，可以作为一个审校单元。学术专著、古诗文读本常有页下注，以页为审校单元可以处理正文与注释是否一致的问题；但同时还要以章节为单元进行审校，以便处理前后不一致的问题。版面复杂的“课堂笔记”、童书、双语对照读物等，需要把文本整理为正确的上下文，尽可能保留元素之间的对应关系，这样大语言模型才能更好地理解。练习册常附有答案，需要把答案与习题合并到一处审校。某些书需要参考权威资料或查字典。另外，每一本书都有自己的特点，审校时需有所侧重，比如儿童读物需要关注价值观是否正确、是否有行为误导等问题，双语对照、注音读物需要关注文本之间的对应关系；通读之外还常要专项核对目录、序号、引文、拼音、人物信息等。这些要求都是市面上的“一键式”审校应用难以妥善解决的。缺的不是再多一个“一键按钮”，而是一种能把审校拆解为清晰环节、让编辑在需要时融入专业判断和手头资料、并能将通读与专项深度审校分开进行的工作方式。

== 三、AI Proofreader简介

基于上述对审校工作痛点和常见“一键出结果、侧栏点标签”审校应用问题的认识，结合日常审校实践，我开发了 AI Proofreader。AI Proofreader 是 VS Code 的一个扩展，二者均为开源软件，代码库均托管在 GitHub 平台。

AI Proofreader 面向图书编辑，可根据稿件的具体需要，结合编辑自身的审校经验进行深度配置；当然，大多数稿件也可按默认流程和参数直接使用。

=== （一）功能与界面

安装 VS Code 与本扩展后，在阿里云百炼或 DeepSeek 开放平台申请接口密钥并填入即可使用；系统提供从安装到生成勘误表的分步说明可供参照。

本扩展安装在 VS Code 中，有三种使用方式：

其一，普通用户和初学者可从左侧活动栏打开侧栏的 AI Proofreader 概览。通过概览可进入校对面板，完成大部分审校工作。在概览界面还可切换侧栏列表的显示，如资料检索、引文核查、重文检查、标题树、提示词等。如#ref(<fig-overview>)所示。

#插图("img/参赛申报书-初稿/1-1.png", [提示词与校对面板]) <fig-overview>

通过概览界面还可进入资料检索面板，为文稿检索本地或在线参考资料。如#ref(<fig-search>)所示。

#插图("img/参赛申报书-初稿/image-2.png", [字词专项检查与检索面板]) <fig-search>

其二，在编辑器界面，可通过文档右键菜单调用与当前文档类型相应的命令。

#插图("img/参赛申报书-初稿/3.png", [文档编辑窗口右键菜单]) <fig-context-menu>

其三，熟练之后，可通过命令面板（Ctrl+Shift+P）输入“AI 校对”或“AI Proofreader”检索并使用全部命令。如#ref(<fig-command-palette>)所示。用户常用命令会排在最前面，不必每次都检索，且多数命令的参数能够保留，因此这种方式更加快捷，特别适合重复操作。

#插图("img/参赛申报书-初稿/4-1.png", [在命令面板中查找全部命令]) <fig-command-palette>

本扩展以三种校对工作流为主干，并辅以文档整理、参考资料检索、专项检查等功能。

==== 1. 三种校对工作流

*（1）选段即时校对*。适合短文、精读或边读边改。在 Markdown 文档中选中一段文字，使用命令 `proofread selection`，或点击相应的校对面板按钮、使用文档右键菜单，即可进行校对。校对完成后会自动打开差异对照编辑器（diff）。如#ref(<fig-diff>)所示。

#插图("img/参赛申报书-初稿/image.png", [在 diff 编辑器中审核原文与改后的差异]) <fig-diff>

*（2）长文档切分后批量校对*。适合一次性处理整本书。在校对面板上大致分为四步（操作界面见#ref(<fig-overview>)）：

+ *文档整理*（可选）：将 Word、活文字 PDF 转换为 Markdown；整理段落，按目录标记标题等。
+ *切分与合并*：选择主文件 → 按长度或标题等条件切分（生成 JSON）→ 需要时合并习题与答案、检索并合并参考资料等。
+ *LLM 校对 JSON*：批量校对切分后的片段；第 1 轮校对后还可使用“重叠校对”功能，基于上一轮全文再校对一至多轮。
+ *查看校对结果*：在 diff 编辑器中比较前后差异（如#ref(<fig-diff>)所示）；还可以生成差异文件、勘误表或加工记录（如#ref(<fig-errata>)所示）。

*（3）带记忆的选段校对*（实验功能）。适合同一部书稿中反复统一体例。选中段落并使用“带编辑记忆”模式校对，核准结果后会自动收集已确认的修改方案和通则，写入编辑记忆文件；后续校对其他选段时会自动加载编辑记忆，减少重复说明。

三种工作流共用提示词与差异审核机制。自定义提示词时，利用同一套机制还可进行翻译、加注、出练习题等各种文本处理工作，不限于审校。实际上，本扩展是一组通用的批量文字处理工作流，编辑可以充分发挥想象，结合自己的专业知识、经验和需求，用它完成各种文本处理任务（参考#ref(<fig-overview>)左侧栏的提示词）。

#插图("img/参赛申报书-初稿/image-4.png", [加工记录/勘误表]) <fig-errata>

==== 2. 界面上的其他功能入口

见#ref(<fig-overview>)、#ref(<fig-search>)，功能入口归类如#ref(<tab-entries>)所示。

#figure(
  table(
    columns: (auto, auto),
    align: (left, left),
    stroke: 0.5pt + luma(180),
    inset: 8pt,
    [*入口*], [*作用*],
    [*校对面板 · 文档整理*],
    [转换格式、整理段落标点、标记标题、对齐标题、分句分词、与另一文件比较——为切分和校对做准备],
    [*校对面板 · 切分与合并*],
    [切分、合并语境；进入“准备参考资料”或“LLM 校对/重叠校对”流程],
    [*校对面板 · 校对结果*],
    [查看进度、全文差异、差异文件、勘误表],
    [*校对面板 · 专项检查*],
    [字词检查与自制正误表、标题树与段内序号、对齐标题、重文检查、引文核对],
    [*校对面板 · 底部*],
    [选段校对、带记忆选段校对],
    [*检索面板*],
    [查词典、工作区文献、可选维基百科等；勾选后可导出或并入参考资料；*只检索，不校对*。备好材料后返回校对面板，可选用“知识核查”功能],
    [*管理提示词*],
    [预置提示词（硬伤、对应关系、拼音、知识核查等）、自定义提示词，以及用于说明整书重点的“文本特性”提示词],
  ),
  caption: [界面功能入口一览],
) <tab-entries>

专项检查及其结果侧栏如#ref(<fig-search>)所示。提示词管理侧栏如#ref(<fig-overview>)所示。

=== （二）开发与使用情况

我是图书编辑，2022 年起关注大语言模型在文本处理方面的表现；2023 年底，鉴于模型已具备实际改稿能力，便针对本人编校工作中的真实痛点着手开发工具。起初构建了 Python 原型库（#link("https://github.com/Fusyong/ai-proofread")[Fusyong/ai-proofread]），验证了主要审校流程；后来将其开发为 VS Code 扩展，以便融入日常改稿环境，并直接利用差异对照、多文件并排、版本管理等成熟功能。扩展代码库见 #link("https://github.com/Fusyong/ai-proofread-vscode-extension")[Fusyong/ai-proofread-vscode-extension]。

本扩展的功能通常是我在处理具体书稿时，为解决实际问题而逐步开发或引入的，如 PDF 与 Word 格式转换、习题与答案拼合、词典与文献检索、自定义提示词、文本特性注入、专项检查与自定义检查表、勘误表生成与错词积累等，并在自用与同事试用过程中反复迭代优化。

本扩展小步快跑，高频迭代。自 2025 年 4 月 5 日发布于 VS Code 应用市场以来，累计更新大小版本 100 多个，平均约 5 天更新一个版本。目前仍在积极开发中。当前版本为 v1.12.23，应用市场安装量为 340。扩展主页如#ref(<fig-marketplace>)所示。

#插图("img/参赛申报书-初稿/image-6.png", [AI Proofreader 插件主页]) <fig-marketplace>

原型库部分代码为手工编写，其余程序实现主要借助 Cursor 编程智能体。审校类提示词由我结合经验撰写，资料检索和记忆库处理等提示词为人机共创。

在扩展测试与实际使用中，主要调用阿里云百炼和 DeepSeek 开放平台的 LLM API。扩展同时支持 Google Gemini API 和 Ollama 本地模型接口。

*使用情况*：

- 我已用本扩展处理百余种书稿，包括协助同事处理的稿件；
- 本社有十余位编辑安装使用；
- 在语文社质检流程中，总编室安排专人使用本扩展，对未经 AI 审校的稿件进行预检；
- 审校速度快，十余万字的书稿，切分后提交审校，通常能在半分钟以内完成；
- 调用阿里云百炼、DeepSeek 开放平台最新高性能 API，在普通校对模式下，每十万字成本约为一至三元；
- 在高性能通用大模型集中于少数头部 AI 公司、行业小模型几乎没有优势的当下，据个人与同事的经验判断，本扩展在调用高性能大模型的情况下，审校效果不输于市面上常见的商业审校应用，且审核负担更轻，审核效率显著更高。

=== （三）评测简报

以上判断仅基于少数人的经验。当前也没有公开的审校测评数据和平台，无法与竞品进行比较。

但为了较为客观地评价本扩展的能力，向用户、评委量化展示本扩展的能力与优化方向，我在 WorkBuddy 的协助下，收集了网上公开的编辑校对试题，使用本扩展审改，最后逐一核对参考答案并进行统计。

==== 1. 测评资料与统计口径

测评试题是网络上公开的近年“韬奋杯”全国图书/出版社青年编校大赛、全国报刊编校技能大赛校样校对、篇章加工类真题，筛除了没有参考答案的部分，以及字体字号版式等无法通过纯文本呈现的部分。

有效试题共 19 篇（组），全部预设有差错，附官方参考答案。先用 6 种配置各校对一次：第 1 次 qwen3.8-max，第 2 次 qwen3.8-max 重复 target（即要修改的文本），第 3 次 qwen3.7-max，第 4 次 qwen3.7-max 重复 prompt（包括要修改的文本和修改要求等），第 5 次 qwen3.7-max 按 600 字符起切分，第 6 次 qwen3.7-max 重复 target。除第 5 次外，各次不加切分，每篇作为整体一次完成校对。模型温度均取 1，关闭思考模式。

逐条比对修改前后文本与参考答案，进行统计。“命中”表示改对采分点，“漏报”表示未处理，“误报”表示把对的改错。纯排版格式（包括表格样式）、文本转换类问题，以及须对照原稿的“对校”类问题，因在纯文本环境下无法呈现、修改、评价，这些条目保留但不计入分母（共剔出 10 个计分点）；校样中的校对类，因没有原稿可对，有改动即计为命中、未改动则剔出分母。有效计分点，第1、5次为285，其余为284。

==== 2. 总体成绩

六种配置下的测评结果如#ref(<tab-eval>)所示。

#figure(
  table(
    columns: 6,
    align: (left, right, right, right, right, right),
    stroke: 0.5pt + luma(180),
    inset: 6pt,
    [*场次*], [*命中*], [*漏报*], [*误报*], [*召回率*], [*正确率*],
    [1 qwen3.8-max], [220.0], [62.5], [4], [77.2%], [98.2%],
    [2 qwen3.8-max·重复target], [228.0], [53.5], [2], [80.3%], [99.1%],
    [3 qwen3.7-max], [224.5], [57.0], [2], [79.0%], [99.1%],
    [4 qwen3.7-max·重复prompt], [218.5], [63.0], [0], [76.9%], [100.0%],
    [5 qwen3.7-max·600字符起切], [223.5], [59.0], [2], [78.4%], [99.1%],
    [6 qwen3.7-max·重复target], [229.0], [54.0], [2], [*81.0%*], [99.1%],
  ),
  caption: [六种配置下的总体成绩],
) <tab-eval>

六场召回率为 76.9%—81.0%（波动约 4 个百分点），正确率为 98.2%—100%。计算机文稿、成语、数学片段多次满分；八套报刊大赛卷按 5 分/处实算得 360—375 分（满分 400），偏低的是版面格式卷（38%—63%）、校样片段卷（50%—67%）、版权页（41%—59%）。

因手头没有这些比赛的成绩数据，无法与真人水平进行比较。凭个人经验，这个成绩大概是很不错的。

==== 3. 主要结论、分析与改进策略

（1）*改得很准，漏掉一些，还要用真实书稿来评估*。失分几乎全部来自漏报，通常漏报20%有零；每场误报仅 0—4 处。就比赛题场景而言，目前的保守倾向有价值，契合“改不误为误倒扣分”的比赛要求；在真实场景中也能有效减少审核负担，但不利于提醒编辑注意。目前的正确率很高，因而召回率还有不少提高空间，因为找出错误后即使没有改对也有提醒价值。不过，*比赛真题并不能完全代表真实书稿的情形*，比赛题错误密度高得多，有人为“设计”的因素。下一步还要在真实书稿上测评。

复现的系统性误报只有一处：书籍版本记录中的印次被误改（“第 7 次印刷”→“第 1 次印刷”，答案只要求改“第 1 版”），第 6 场两处误报全部属此类型。

需要说明的的是，因为没有标准供参考，参考答案之外的改动一律忽略。从上述正确率以及平时使用经验看，这些地方被改错的可能性相当小，多半可以作为优化提醒。实际上，*有若干答案外改动经核对属正确修正，如“通俗相尚”改为“风俗相尚”*，因不在采分点内，不计分也不计误报。

*值得注意的是：* ①单纯重复目标文本能提高召回率，且不影响正确率；而重复完整的提示词（包括目标文本和修改要求等），却导致召回率降低，但正确率提高。——也许较长且结构交替的文本会影响对文字细节的注意，但能提高对逻辑关系的注意。②切分为 600 多字符的短片段（19 篇中有 5 篇在 1000 字到 3038 字之间）几乎不影响校对效果，还白白增加了 token 消耗。——需要用真实书稿来测试，确定多长的文本会影响效果。

（2）*短板三类，需要有针对性地补强*：①知识、数据核实（数字、年份、版本记录）。可见专项核查、检索资料合并校对很有必要，后续有必要测评这些部分。②“提出质疑即可”的题型——本扩展只在“改/不改”间二选一，通常不输出存疑建议（条目式输出时有说明）。有必要设置专门提示，对非具体字词错误类问题进行质疑、提醒，供用户参考。③计算类全局动作（汇总差错数、算差错率）六次全漏。计算类书稿，比如数学练习册，需要使用专门的提示词，也可尝试打开思考模式。

（3）*qwen3.7-max 略优于 qwen3.8-max*：前者为纯文本模型，后者为图文影像模型，能力差异不大。但从实际经验来看，前者的普通知识或词语细节专注力似乎更好，建议优先使用。

== 四、辅助编辑挖掘自身专业知识与经验做好审校工作

AI Proofreader不是“一键出结果、侧栏点标签”式的传统审校应用，它把审校拆成几个清晰的环节，配上校对面板和一批辅助工具，供编辑选用。一般书稿按预置流程和默认参数即可跑通：转换或打开Markdown文件 → 切分 → 校对 → 看差异。遇到特殊版面或特殊要求时，再按需整理文本、调整切分方式、换提示词或补参考资料；通读之外若还要做专项核对，则用校对面板里的专项检查，或换用专项提示词再跑一轮。如此，最大限度地利用编辑的专业知识、经验和手头资料，并充分发挥大模型与规则工具各自的能力。

=== （一）文本整理与语境组织：稿件是什么样子，怎么读，由编辑定

“一键式”应用往往在解析和送审时替编辑做完决定：PDF 文字流怎么提取、按什么单位切、题目与答案分不分开、参考资料用不用……本扩展把选择权交还给编辑，但并不要求编辑从零手工拼出一份“完美语料”。日常路径很短：转换文档、必要时点几下整理，再按长度或标题切分即可开校。

*先整理成可审的文本。* 来稿若是 Word，或排版端导出的活文字 PDF，用扩展内命令转成 Markdown 即可；Windows 下 PDF 转文本功能已随扩展附带，不必另装。死文字 PDF（文字已转曲或转成图像）仍须先做识别，这一步扩展不代劳。转出后若分段不清、断行分页碍眼，可用整理段落、按目录标记标题、对齐标题等命令清除杂质；不必追求排版级完美——大模型对略有瑕疵的文本仍有较好的兼容与理解力，整理到“读得顺、切得开”往往就够。复杂版面的“课堂笔记”、儿童书、双语对照等，才需要多花一点工夫理清元素对应；“一键解析 PDF”省掉的，恰恰是这一步，也最容易引入误报误改。

*再按目的组织语境。* 一般语言文字校对，按长度切分（经验上六七百字一段）即可。要查前后一致，再改用按章切分，或给短段配上章节上下文。儿童读物可按页或按段；带页注的学术稿、古诗文读本，可先按页看正文与注释，再按章看全书。练习册可将习题与答案拼进同一段，必要时并入配套课文。需要查证时，把词典、权威注释或文献拼进参考资料；手头没有现成摘录时，可用下一节的检索面板去查、勾选后再并入。模型每次都能看到目标文本，以及可选的上下文、参考资料。这些不是每本书都要全做：编辑对自己常做的那几种稿，摸清一两次之后，便形成固定习惯，后面处理同类稿会越来越省事。

整理文本确有琐碎之处，但大模型并不要求完美无瑕的语境；本扩展在转换、整理、切分、合并上提供的辅助工具，多数已在长期使用中磨合顺畅。因此，一般书稿先走默认流程即可；遇到特殊版面或特殊要求时，再整理文本、调整切分、拼上手头的教材、词典或文献——把编辑的专业知识和已有材料用起来，用较小的整理成本，尽量发挥大模型的审校能力。深度配置是加分项，不是入门门槛。

=== （二）参考资料检索：帮编辑快速查工具书和查参考资料

这是语境组织里很常见的一类特例：不是把已有文件直接拼进去，而是先替编辑把该查的工具书和参考资料找出来，再由编辑决定用不用、用哪几条。对应扩展里的*检索面板*（在校对面板切分区块也可点“准备参考资料”进入）。检索只负责找材料和导出、合并，*不做校对*；校对仍回到校对面板，需要时再选用“知识核查”等提示词。

可勾选的来源包括：本地 MDX 词典、工作区里的参考资料、维基百科网站等。既可按当前切分好的书稿批量准备，也可对选中段落做意图检索或直接查词典。命中结果经过AI打分、排序、筛选后，交由编辑再次筛选，然后再合并进校对用的参考资料栏。

与“一键式”应用里厂商申明自带知识库但编辑看不到依据不同，这里查什么、信哪一条、并入哪一段，都有AI辅助，最终决定权则在编辑手里。

=== （三）预置提示词 + 自定义提示词 + 文本特性提示词：怎么审校，重点关注什么，由编辑定

审校时“怎么做”和“这部稿要特别盯什么”，都由编辑用提示词规定，而不是交给“一键处理”。

*多种预置提示词，反应最佳实践，满足通用场景。* 名家力作宜用“硬伤发现”，少动可改可不改处；文字较差的稿子可再加一轮“表述正常化”。两轮分开做，比一键大改更好审。另有常规语言文字校对、对应关系核对、知识核查、拼音审校等，在校对面板里点选即可。通读之后若要对注释对应、拼音、专名等做强化，换一条专项提示词再跑一轮即可，与第一节的“专项审读”相衔接。已经用检索面板备好参考资料时，可选用“知识核查”，要求模型依据已给材料判断，而不是凭空修改。

*自定义提示词：按个人经验指导模型工作，不限于审校工作。* 若某类书反复碰到同一类问题，可把编辑自己的经验写成提示词（如儿童读物的价值观、注音读物的对应关系），写好后可一直沿用。同一套机制还可做翻译、加句读和注释、设计练习题、格式整理、意识形态风险提示等。我已在 GitHub 仓库 #link("https://github.com/Fusyong/LLM-prompts-from-a-book-editor")[Fusyong/LLM-prompts-from-a-book-editor] 上分享十数种提示词。选中一段即可校对；体例要统一时，也可带上已确认的通则，免去每段重复说明。

*文本特性提示词：提醒模型重点关注什么。* 系统默认提示词和若干预置提示词在校对时可再注入源文本特性说明：这部书（或这一批稿）有什么独特之处，审校时要特别留心什么，如体裁、读者对象、注音体例、不宜改动的部分等。目的是提醒模型：当前片段只是更大源文本的一部分，请按整书特性把握分寸。

=== （四）改前改后全文对照审核，效率远超侧栏标签说明与版面锚点审读模式

模型通常返回改后全文。审核时打开 VS Code 的差异对照编辑器：左侧改前删除的文本着深红，右侧改后插入的文本着深绿。思维方式接近编辑熟悉的校后通读：顺着最终文本看，遇到变色处，找出左侧对应改动，判断留下还是改回。不必在侧栏里逐个打开标签，阅读说明文字，对着着色锚点想象改完后的句子，也没有锚点叠压分不清的麻烦。确认后誊到纸稿；*除非对电子稿有十分把握，否则不把电子稿直接灌入排版软件。*

=== （五）核红快捷可靠，版本全程可追溯，审改档案可复用

排印样回来后，除了对着纸质红样核红，还可以将 PDF 转文本与上一版电子文本对照，核实上次修改是否进了排版文件，还可以看出是否有多余改动。

根据修改前后的电子文本，还能一键生成加工记录，也可以手动筛除不重要的改动；还可导出常用错词正误对，积少成多，积累成自己的常用检查库。原稿、修订稿和差异文件都留在文件夹里，占用空间小、可检索，可作为语料用于训练企业自有模型，也比堆积纸质过程稿省事省空间。

稍加学习，还可以通过 VS Code 和 Git 管理稿件版本，顺手留下“初稿、作者修改、一校、二校、三校、通读、核红”这样的标签，减少文件数量和版本混乱，做到全流程修改可追溯。

=== （六）专项检查：有针对性的强化审校

上面说过，除从前到后的审改外，通常还要专项核对目录、页眉页脚、栏目名称，或对注释、拼音、人物信息等做强化审校。一键审校很难按编辑指定的项目做深；本扩展把这类工作放在校对面板的“专项检查”里，与大模型通读分工：规则能定的用规则扫，语义与知识仍交给模型或专项提示词。

用到时在面板点选即可：

- *字词检查*：涵盖词典、规范汉字与异形词，以及编辑自制的正误表、人物与专名表等，适合人物信息、用字规范的专项强化。有心的编辑平时可以通过本扩展收集词语级别的正误对，或用正则表达式编写，形成自己的正误表，以此积淀经验，用于以后的审校工作。我及网友已经在 GitHub 仓库 #link("https://github.com/Fusyong/regexp-replace-lists-for-TextPro")[Fusyong/regexp-replace-lists-for-TextPro] 分享了几十种检查替换表。
- *标题树与段内序号*：查标题层级与序号连续性，对应目录、章节序号、列表序号一类问题。
- *对齐标题*：目录与正文、题本与答案并排时，检查标题是否一一对应。既能检查一致性，还为语境拼接做好了准备。
- *重文检查*：扫描重复句，减轻通读时对“似曾相识”内容的遗漏。
- *引文核对*：与本地资料库做相似度匹配，辅助引文核验。

本扩展主要用于文本处理。此外，我还开发了 PDF 版面差异核对工具 #link("https://github.com/Fusyong/PDFDiffer")[Fusyong/PDFDiffer]，用于检查 PDF 版面图文的微小变化（包括文字改动）；以及 PDF 批量正则搜索聚合查看工具 #link("https://github.com/Fusyong/PDFSearchViewer")[Fusyong/PDFSearchViewer]，用来批量检查 PDF 元素的一致性，如同级标题、栏目文字表述和版式是否一致。推荐同仁使用。

简言之，本扩展预置流程保证普通稿开箱即用，需要时再动用整理、切分、资料检索、自定义提示词和专项检查等功能，从而让编辑来规定模型读什么，怎么读，怎么改，重点改什么，大模型做不到的地方编辑再专门处理。如此，编辑可以挖掘自己的专业知识与经验，利用手头资料，还能促进专业成长，避免AI依赖。

== 五、限制与风险提示

本审校扩展在编校流程中的定位始终是“辅助”，而非“替代”，编校人员始终是决策者和责任者。在通常模式下，扩展给出修改建议时不会附带说明；即使个别情形下给出了改动说明，编校人员也必须查证、判断，再誊抄到纸稿上，并在修改后核红。手动“誊抄到纸稿”可以看作一种物理隔离机制。如果直接发排经本扩展审校过的电子稿，则后续的完整编校流程绝对不能缺漏。

本扩展调用的是通用大语言模型，可能会遗漏问题、过度修改，甚至把正确的内容改错，因此绝对不应盲目采纳或批量采纳。

PDF 转文字后，文字整理并非必需，但整理得当能提高审校效果，减轻审校负担。尤其是题目与答案对齐、语境拼合等工作，能大大提高审校的可靠性。

对于某些复杂版面的 PDF，比如单栏与双栏交替且原始文本流无法正确导出的情况，本扩展工具可能无法处理，此时可以使用我改进过的开源 OCR 工具 #link("https://github.com/Fusyong/RapidDoc")[Fusyong/RapidDoc] 等进行处理。

单次审校往往存在局限，建议至少处理两轮：一轮按默认长度（六百多字）审校，以保证模型有足够的注意力审改字词句；另一轮按章节标题等完整语境审校，以处理一致性问题。模型每次处理都有上下文长度限制，问题较多的稿件可以重叠审校多次（最后由人工统一审核最终改动）；“重复提示词”功能也能提高审校的密度和精度。

出于成本考虑，本扩展暂不支持图片审校，后续可能会支持。

本扩展没有后台服务器，不会向开发者发送任何数据。用户的 LLM 平台密钥保存在用户本机，用户应妥善保存并定期更换。本扩展会将书稿文本发送到所选的 LLM 平台进行处理，用户应了解所选平台的协议与规则，并注意涉密问题。

== 六、对相关权利和人工智能使用方式的说明

本扩展的核心架构、流程与编校提示词由我原创，编辑记忆、资料检索规划等环节的提示词，以及应用开发全程，由我在编程智能体Cursor辅助下完成。字词检查所用的规范数据从互联网公开资料中获取，并经我整理。测试时主要调用阿里云百炼、DeepSeek 开放平台当时最优的文本模型。

本扩展的性能测评部分，使用办公智能体 WorkBuddy 收集、整理了互联网上公开的“韬奋杯”全国图书/出版社青年编校大赛、全国报刊编校技能大赛的篇章类试题，并由 WorkBuddy 根据原题答案和评分标准完成了逐个评分点的核对与评分；试题范围由我确定，评分点核对结果与评分明细表格均经过我核查。

本扩展依赖的运行环境、工具和库如#ref(<tab-deps>)所示，其权利归原权利人所有，本扩展仅在其许可下调用或分发。

#figure(
  table(
    columns: (auto, auto, auto),
    align: (left, left, left),
    stroke: 0.5pt + luma(180),
    inset: 7pt,
    [*组件或服务*], [*用途*], [*说明*],
    [Visual Studio Code], [编辑器、差异对照、版本管理、扩展运行环境], [开源编辑器；本扩展遵循其机制],
    [Node.js], [扩展运行时], [要求 ≥18.12.1（随 VS Code 扩展宿主提供）],
    [大模型接口], [按提示词生成修订文本、资料检索规划等],
    [支持阿里云百炼、DeepSeek、Google Gemini、Ollama 等，由使用者自备密钥并遵守各平台条款；书稿文本将发送至所选平台],
    [Pandoc], [Word 与 Markdown 互转], [须使用者另行安装],
    [Xpdf（pdftotext）], [Windows 下活文本型 PDF 转文本], [GPL 协议，随扩展分发（许可文本在扩展内）],
    [jieba-wasm], [分词与词频统计], [用于整理与统计，集成于本扩展],
    [jsdiff（`diff`）], [行内差异展示、逐句对齐勘误表等], [开源库，随扩展打包],
    [mdict-js], [本地 MDX 词典查询], [用于资料检索，集成于本扩展],
    [sql.js], [本地参考资料索引（检索、核对引文）], [集成于本扩展],
    [`@vscode/ripgrep`], [工作区文献关键词检索], [用于资料检索，随扩展分发],
    [opencc-js], [繁简转换、地区用字转换], [用于文档整理，集成于本扩展],
  ),
  caption: [依赖的运行环境、工具和库],
) <tab-deps>
